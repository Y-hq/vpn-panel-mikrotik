```
  ███████╗ ██████╗ ██╗     
  ██╔════╝██╔═══██╗██║     
  ███████╗██║   ██║██║     
  ╚════██║██║   ██║██║     
  ███████║╚██████╔╝███████╗
  ╚══════╝ ╚═════╝ ╚══════╝
            S0L
```

[🇬🇧 English Version](README.md)

# پنل مدیریت VPN برای Mikrotik

یک پنل سبک، مدرن و کاملاً داکرایز شده برای مدیریت کاربران VPN روی **روترهای میکروتیک**.

این نسخه با تکنولوژی‌های زیر ساخته شده است:

- **Backend:** Node.js + Express  
- **Frontend:** React + TailwindCSS  
- **Database:** PostgreSQL  
- **Reverse Proxy:** Nginx  
- **Deployment:** Docker Compose  

این پنل یک API ساده و یک رابط کاربری سبک برای مدیریت کاربران VPN ارائه می‌دهد.

---

## 🚀 امکانات

- افزودن کاربر VPN  
- غیرفعال / فعال کردن کاربر  
- حذف کاربر  
- ذخیره کاربران در PostgreSQL  
- اجرای کامل با Docker (فرانت + بک‌اند + دیتابیس + Nginx)  
- ساختار تمیز و ماژولار در Backend  
- رابط کاربری مدرن با React  

---

## 📂 ساختار پروژه

```
vpn-panel-mikrotik/
├─ backend/
│  ├─ src/
│  │  ├─ app.js
│  │  ├─ routes.js
│  │  ├─ db.js
│  │  └─ controllers/
│  ├─ package.json
│  ├─ Dockerfile
│  └─ .env.example
│
├─ frontend/
│  ├─ public/
│  │  └─ index.html
│  ├─ src/
│  │  ├─ App.js
│  │  ├─ Users.jsx
│  │  ├─ index.js
│  │  └─ index.css
│  ├─ package.json
│  └─ Dockerfile
│
├─ nginx.conf
├─ docker-compose.yml
└─ README.md
```

---

## 🛠 پیش‌نیازها

فقط نیاز دارید به:

- **Docker**
- **Docker Compose**
- روتر Mikrotik (اختیاری — برای تست لازم نیست)

---

## ⚙️ شروع کار

### **۱. کلون کردن مخزن**

```bash
git clone https://github.com/Y-hq/vpn-panel-mikrotik.git
cd vpn-panel-mikrotik
```

---

### **۲. ساخت فایل `.env` برای Backend**

```bash
cp backend/.env.example backend/.env
```

سپس مقداردهی کنید:

```env
DB_HOST=db
DB_USER=vpn_user
DB_PASSWORD=vpn_pass
DB_NAME=vpn_db
```

---

### **۳. اجرای پروژه با Docker**

```bash
docker-compose up --build
```

سرویس‌ها اجرا می‌شوند:

- **frontend** → روی پورت **3000**  
- **backend** → پشت Nginx  
- **db** → PostgreSQL  
- **nginx** → روی پورت **80**  

---

### **۴. دسترسی به پنل**

فرانت‌اند:

```
http://localhost:3000
```

API بک‌اند (از طریق Nginx):

```
http://localhost/api/users
```

---

## 📡 API Endpoints

### دریافت لیست کاربران
```
GET /api/users
```

### ایجاد کاربر
```
POST /api/users
{
  "username": "test",
  "password": "1234"
}
```

### غیرفعال کردن کاربر
```
PATCH /api/users/:id/disable
```

### فعال کردن کاربر
```
PATCH /api/users/:id/enable
```

### حذف کاربر
```
DELETE /api/users/:id
```

---

## 🧩 نکات توسعه

### اتصال واقعی به Mikrotik
برای افزودن ارتباط واقعی با RouterOS، این فایل را ویرایش کنید:

```
backend/src/controllers/mikrotik.js
```

---

### اجرای Backend بدون Docker (اختیاری)

```bash
cd backend
npm install
npm start
```

---

## 📜 لایسنس

این پروژه متن‌باز است و آزادانه می‌توانید آن را تغییر دهید.

---

## ❤️ سازنده

ساخته شده توسط **Y-hq**  
در صورت نیاز می‌توانید Issue باز کنید یا مشارکت کنید.
